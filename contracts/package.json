{
  "name": "trusted-ai-audit-platform-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node --hostname 127.0.0.1",
    "deploy": "hardhat run scripts/deploy.cjs --network localhost"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "ethers": "^5.7.2",
    "hardhat": "^2.22.19"
  }
}
